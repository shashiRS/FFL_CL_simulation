'version': 1.0
'validators':
    'git_commit_validator':
        # Optional parameter to switch on/off check of email-domains
        'check_email_addresses': True
        # Optional parameter to switch on/off check of jira ids in commit message for feature branches
        'check_commit_for_jira': False

    'jira_issue_validator':
        'jira_configuration':
            'LSM200':
                'server': 'https://jira-adas.zone2.agileci.conti.de'
                'jql': 'status != Done'
            'SYS230TM14':
                'server': 'https://jira.auto.continental.cloud'
                'jql': 'status != Done'
            'PLP':
                'server': 'https://jira.auto.continental.cloud'
                'jql': 'status != Done'
            'DFMFPT':
                'server': 'https://jira-adas.zone2.agileci.conti.de'
                'jql': 'status != Done'
            'MOCO':
                'server': 'https://jira-adas.zone2.agileci.conti.de'
                'jql': 'status != Done'
            'VDE':
                'server': 'https://jira-adas.zone2.agileci.conti.de'
                'jql': 'status != Done'
            'SIMEN':
                'server': 'https://jira-adas.zone2.agileci.conti.de'
                'jql': 'status != Done'
            'PAR210':
                'server': 'https://jira.auto.continental.cloud'
                'jql': 'status != Done'
                
            'SYS230TM14':
                'server': 'https://jira.auto.continental.cloud'
                'jql': 'status != Done'  

# log_level is used to configure the amount of log messages from the pre-receive-hook
# supported levels are DEBUG,INFO,WARNING and ERROR
# Wrong project config setting will enable default level INFO
'log_level': 'INFO'
